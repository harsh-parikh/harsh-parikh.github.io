# Repository working preferences

- After completing and verifying requested repository changes, create a Git commit with a concise, descriptive message unless the user explicitly asks not to commit.
- Do not push commits to a remote unless the user explicitly asks for a push.
